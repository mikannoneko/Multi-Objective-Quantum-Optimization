检查结论：当前项目已完成可运行的 PyTorch 小规模算法流程，但尚未达到 [MindSpore community #2075](https://atomgit.com/mindspore/community/issues/2075) 的验收要求。

检查日期：2026-09-06。检查基线：Git commit `0e77d1e`，开始检查时工作树干净。本次没有修改算法源码；新增本报告，验证输出保存在 `.tmp_test/review_20260906/`。

任务原文通过该公开 issue 的只读接口取得，要求：基于 MindSpore 实现 FM 并验证原始函数行为一致、复现 Figure 4 的 CGFM 效果、复现 Figure 5 的多目标效果。以下按这一目标评估，而非仅采用当前 README 自定的 quick 验收范围。

| 优先级 | 检查发现 | 对任务的影响 |
| --- | --- | --- |
| P1 | FM 后端仍为 PyTorch | 不满足任务明确要求的 MindSpore 实现 |
| P1 | 已有实验只能支持部分论文趋势 | Figure 4/5 的效果一致性证据不足 |
| P2 | 共晶点附近 ΔT 会返回负值 | 合法组成可产生非物理的优化目标 |

**1. 验收标准问题： FM 后端与任务要求不符。**

[fm_torch.py](<C:/a_personal_file/codex project/fm_torch.py:51>) 定义的是 `TorchFMRegressor(nn.Module)`，使用 PyTorch 张量、自动微分和 LBFGS；[experiment_runtime.py](<C:/a_personal_file/codex project/experiment_runtime.py:42>) 将训练后端固定为 `pytorch_fm_lbfgs`。[environment.yml](<C:/a_personal_file/codex project/environment.yml:1>) 也没有 MindSpore 依赖。Figure 4/5 均直接调用这一实现。

现有测试验证了 FM 到 QUBO 的能量展开关系，但没有 MindSpore 实现及其与原始函数的对照证据。应先补齐 MindSpore 后端，并对固定参数、固定输入下的 FM 输出与参考表达式进行独立验证；训练行为的等价范围也应明确记录。

不知道咱们项目验收标准是否严格，我这两天需要先实现mindspore后端，还是先跑paper呢？

**2. 实验效果：未运行paper规模，已有实验部分证明 Figure 4/5 的效果一致，但不充分。**

已核对的参数差异如下。论文来源为 [正文 2.2 与补充材料 S1](https://arxiv.org/html/2512.11479v1)。

| 参数 | 当前已完成的 quick 实验 | 论文实验 |
| --- | --- | --- |
| FM 训练 | PyTorch + LBFGS | fastFM + ALS |
| Figure 4：每变量 levels / 迭代 / 数据集重复数 | 10 / 100 / 3 | 50 / 600 / 20 |
| Figure 5：每变量 levels / 迭代 | 25 / 150 | 25 / 1000 |
| Optuna trials | 3 | 20 |
| SA reads / sweeps | 100 / 500 | 1000 / 3000 |
| one-hot penalty γ | 2.0 | 1.0 |

Figure 4 当前 3 个 seed 的最终均值：

| 目标 | w/ CGFM | w/o CGFM |
| --- | ---: | ---: |
| κ，越大越好 | 154.521891 | 154.009217 |
| E，越大越好 | 83.671217 | 83.198056 |
| ρ，越小越好 | 2.560000 | 2.568000 |
| Δα，越小越好 | 0.005141 | 0.027443 |
| ΔT，越小越好 | 0.193214 | 5.348000 |

前三项支持 CGFM 的优势方向。2026-09-06 的补充枚举确认：ΔT 的两个终值分别是当前直接编码域与 CGFM 编码域并集的全局最小值，约 27.68 倍的终值差距来自两种参数化的可达最优值不同；橙色 Δα 也已达到直接编码域的全局最小值。因此，这些平台本身不能作为 FM 训练或 SA 失败的证据。当前结果支持小规模流程按编码域进行优化；与论文实验的整体效果一致性仍需在相应配置下验证。

我认为这是参数造成的误差，不影响figure4复现的成功结论。可以通过paper规模运行来解决这个问题。

**Figure 4 平台与阴影的补充核查（2026-09-06）。**

证据来自 [当前 summary](<C:/a_personal_file/codex project/figure4_quick_gamma2/figure4_summary.json>)、同目录 `trajectories/{setting}_{objective}_seed_{seed}.json` 中的初始 100 行与历史最优记录，以及 [绘图逻辑](<C:/a_personal_file/codex project/plot_figure4.py:134>)。适用配置为 10 levels、100 轮、seeds `[0, 1, 2]`、`gamma=2.0`。

枚举方法：直接编码遍历四个非负整数 count，要求总和为 10，再除以 10 得到组成；CGFM 遍历三个 count 的 `0..10` 取值，以 `count × π/20` 得到角度，并遍历 24 种相变量排列。后者包含 `11^3 × 24 = 31944` 个编码与排列组合，其中可以有重复组成，代表所有排列的可达域并集。使用项目中的组成映射与真实性质函数比较目标值，得到：

| 编码域 | Δα 最小值 | ΔT 最小值 |
| --- | ---: | ---: |
| 直接编码，286 个可行组成 | 0.027442827442829554 | 5.347999999999999 |
| CGFM，所有相变量排列的可达域并集 | 0.001141590012551319 | 0.19321373938900877 |

这是运行后的性质核查，不是 QUBO 能量诊断，也没有用枚举候选替代 SA。单轮 CGFM 使用的仍是当轮 seed 确定的一种排列。

直接编码的 Δα 最优组成为 `[0.3, 0, 0.6, 0.1]`；seed 0/1/2 分别在第 5 轮、初始数据、第 1 轮达到该值。直接编码的 ΔT 最优位于实际 Si 体积分数 `0.12`，共有 15 个组成达到；三个 seed 的初始数据均已包含该最优值。10-level 的实际体积分数步长为 `0.02`，无法表示共晶点 `0.128`，因此后续搜索不能将橙色 ΔT 降到零。

CGFM 的 ΔT 在 seed 0/1/2 中分别于第 11/70/16 轮达到 `0.19321373938900877`。曲线绘制历史最优值，阴影统计同一轮三个 seed 的差异；因此橙色 Δα 从第 5 轮起、橙色 ΔT 全程、蓝色 ΔT 从第 70 轮起均满足 `min=max`、`std=0`，阴影收缩到均值线上。蓝色 Δα 的最终 min-max 仍为 `[0.002360781619376695, 0.009828545237954955]`，std 为 `0.0033336318039845943`；该阴影仍存在，且三个 seed 都没有达到枚举得到的 CGFM Δα 最小值。

这项证据明确了当前平台的原因，也限定了“复现成功”的含义：当前 quick 流程的直接编码 Δα、ΔT 及 CGFM ΔT 已达到对应编码域的最优值，CGFM Δα 仍有改进空间；提高 levels 会改变可达域，但 paper 配置下的收敛、采样可行性和效果一致性仍需实际运行验证，不能预先保证。

Figure 5 当前 seed 0 的结果：

| 指标 | w/ DDTS | w/o DDTS |
| --- | ---: | ---: |
| 唯一 QUBO proposal 数 | 148 | 146 |
| 命中精确 Pareto front 的组成数 | 77 | 96 |
| 精确前沿覆盖率 | 5.8511% | 7.2948% |
| 精确前沿 precision | 52.0270% | 65.7534% |
| spacing CV，越小越均匀 | 0.4905 | 0.7330 |

精确前沿包含 1316 个离散组成。DDTS 的 spacing CV 较好，但覆盖率和 precision 较低；150 轮、单 seed 的结果不足以证明论文描述的整体前沿效果。覆盖率仅是当前诊断指标之一，也不能单独用于断言 DDTS 算法失效。

这我认为仍然是规模问题，后续通过paper规模运行解决。

上述数值均来自本次重新运行验收器的 [Figure 4 报告](<C:/a_personal_file/codex project/.tmp_test/review_20260906/figure4_validation.json>) 和 [Figure 5 报告](<C:/a_personal_file/codex project/.tmp_test/review_20260906/figure5_validation.json>)，并已查看两个现有 PNG。

**3. ΔT 在共晶点附近存在确定的数值边界问题。**

[alloy_dataset_generator.py](<C:/a_personal_file/codex project/alloy_dataset_generator.py:236>) 只对距离 `fSi=0.128` 不超过 `1e-12` 的输入返回零，其余输入直接使用舍入后的分段直线。实际调用得到：

| Si 体积分数 | ΔT |
| --- | ---: |
| 0.12799 | -0.002104 |
| 0.12800 | 0 |
| 0.12801 | -0.006911 |

合法归一化组成 `[0.63995, 0.36005, 0, 0]` 经 `build_dataset_row` 也能产生负 ΔT。由于 ΔT 按最小化处理，负值会被视为比零更优。当前测试主要覆盖端点、有限性和非法输入，未覆盖共晶点两侧的这种情况。

建议保留论文系数并对结果设置非负下界，同时增加共晶点左右邻域的定向测试；应记录这属于对舍入系数的物理边界修正。README 已提到该问题，本次对代码中的实际行为作了复现；没有证据表明它影响了当前保存的 quick 曲线。

这可能是导致我的figure4图中 ΔT 曲线像一条直线的原因？
而且为什么没有min-max 、 std？

补充核查答复：当前正的 ΔT 平台分别等于两种编码域的最小值，水平线由已达到网格最优解释；共晶点邻域的负值边界问题与这次平台无关。`min-max`、`std` 已由绘图器生成，在上述收敛区间内因三个 seed 相同而宽度为零。各条曲线的轮次与非零阴影见前面的补充核查。

建议执行顺序：补齐 MindSpore FM 及原函数对照 → 修正 ΔT 边界 → 在固定代码版本下验证训练与求解配置 → 完成 Figure 4/5 效果对照和独立验收 → 按 issue 指定的 mindquantum 仓库准备提交。
